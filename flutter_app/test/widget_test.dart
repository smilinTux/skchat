// Basic smoke test — verifies the package imports resolve correctly.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('package smoke test', () {
    // Placeholder: actual widget tests require a running ProviderScope
    // and initialized Hive/notifications which need mocking.
    expect(1 + 1, equals(2));
  });
}
