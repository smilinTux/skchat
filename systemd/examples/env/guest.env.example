# %h/.config/skchat/guest.env --- shared guest-link token secret.
# Referenced by skchat-daemon.service.d/guest.conf and
# skchat-webui@lumina.service.d/guest.conf. Both MUST see the same value so
# guest links verify across the daemon and the webui. Never commit.
SKCHAT_GUEST_TOKEN_SECRET=
