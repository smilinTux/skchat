# %h/.config/skchat/memory-pg.env --- skmem-pg (pgvector) connection string.
# Referenced by the telegram bridge override.conf drop-ins (opus + lumina).
# Contains a password: never commit. Rotate it per plan task P1.2.
SKMEMORY_PG_DSN=postgresql://USER:PASSWORD@HOST:5432/skmemory
