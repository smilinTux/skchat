# webui-chef.env  ->  ~/.config/skchat/webui-chef.env   (chmod 0600)
# EnvironmentFile for the Chef webui instance (skchat-daemon-chef path).
#
# NAMES ONLY. Secrets are skvault placeholder tokens resolved by
# deploy/provision-secrets.sh. CHANGE_ME lines are host-specific. Never commit
# a filled-in copy.

# ---- non-secret: local service wiring ----
SKCHAT_PORT=8792
SKCHAT_HOST=0.0.0.0
SKCHAT_HOME=CHANGE_ME_isolated_home_e.g._~/.skchat-chef
SKCHAT_IDENTITY=chef@skworld.io
SKCHAT_STT_URL=http://192.168.0.100:18794/v1/audio/transcriptions
SKCHAT_TTS_URL=http://localhost:18797/v1/audio/speech

# ---- non-secret but host-specific ----
SKCHAT_LIVEKIT_URL=ws://100.108.59.57:7880
SKCHAT_LIVEKIT_DEFAULT_ROOM=chef
SKCHAT_PAIRING_REQUIRE_GATE=1
SKCHAT_FUNNEL_PUBLIC_URL=CHANGE_ME_public_funnel_url
SKREACHD_ENABLED=1
SKREACHD_OPERATORS=CHANGE_ME_operator_ids
SKCHAT_TURN_URLS=turn:CHANGE_ME_turn_host:3478

# ---- non-secret: LiveKit API key id ----
SKCHAT_LIVEKIT_API_KEY=skchat-chef

# ---- secrets (provisioned from skvault) ----
SKCHAT_LIVEKIT_API_SECRET=${skvault:skchat LiveKit API Secret (chef)}
SKCHAT_TURN_SECRET=${skvault:skchat coturn TURN Secret}
